# Third-Party Packages

This directory contains source packages maintained outside this project.

- `ldlidar_stl_ros2/`: LDROBOT LiDAR ROS 2 driver used for the XGO LD19.

Keep project-specific nodes under `src/`. When updating vendored code, record
the upstream repository and revision in the update commit and avoid mixing
unrelated project changes into the vendor subtree.
