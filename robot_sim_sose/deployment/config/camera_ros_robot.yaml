/**:
  ros__parameters:
    # camera_ros on the robot rejected the short name "ov5647".
    # The package's own launch file uses "0" as the default camera selector.
    camera: "0"
    format: "BGR888"
    width: 640
    height: 480
    # Add a real calibration file after calibrating the lens.
    # camera_info_url: "file:///workspaces/robot_sim_sose/deployment/config/ov5647_640x480_calibration.yaml"
