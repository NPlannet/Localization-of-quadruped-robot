/**:
  ros__parameters:
    # Do not set the `camera` parameter when only one camera is connected.
    # camera_ros then selects the detected camera automatically. The "0:" shown
    # in its camera list is a display index, not a valid camera name.
    format: "BGR888"
    width: 640
    height: 480
    # Add a real calibration file after calibrating the lens.
    # camera_info_url: "file:///workspaces/robot_sim_sose/deployment/config/ov5647_640x480_calibration.yaml"
