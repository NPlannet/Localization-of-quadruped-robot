# Robot Deployment

The deployment docs and deployment-specific scripts now live under `deployment/`.

Main docs:
`deployment/README.md`

Compatibility entrypoints still exist under `scripts/`, so existing commands like
`bash scripts/sync_robot_runtime.sh ...` and `bash scripts/robot_nav_tmux.sh ...`
continue to work.
